"""
A task scheduler supporting
one-time and recurring jobs,
cancellation,
concurrent execution
and retries.
"""


def main() -> None:
    task = """
A task scheduler supporting
one-time and recurring jobs,
cancellation,
concurrent execution
and retries.
"""

    print(task)


if __name__ == "__main__":
    main()
